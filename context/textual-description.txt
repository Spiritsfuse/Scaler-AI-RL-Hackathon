

This header component is light-themed (`bg-white`) and is structured into two distinct horizontal rows.

### Row 1: Channel Title & Actions

This row is a single `flex` container with `items-center` and `justify-between`. It has horizontal padding (e.g., `px-6`) and vertical padding (e.g., `py-3`).

* **Left Side (Channel Info):**
    * This is a `flex items-center gap-2`.
    * **Favorite Icon:** A `Star` icon from Lucide React. It is not filled, (e.g., `stroke-gray-700 fill-none`).
    * **Channel Name:** The text **"# general"**. This is a `span` or `h1` with `font-bold` and `text-xl` (e.g., `text-slate-900`).

* **Right Side (Channel Actions):**
    * This is a `flex items-center gap-4`.
    * **Member Avatars:** A component showing a stack of overlapping user avatars (images) next to a number, "96".
    * **Huddle Button:** A `button` component with a light border (e.g., `border border-gray-300 rounded-md`). It is a `flex items-center gap-2 px-3 py-1.5`.
        * **Icon:** `Headphones` (Lucide).
        * **Text:** "Huddle".
        * **Icon:** `ChevronDown` (Lucide) for the dropdown.
    * **Search Button:** An icon-only button containing a `Search` icon (Lucide).
    * **More Button:** An icon-only button containing a `MoreVertical` icon (Lucide).

---

### Row 2: View Navigation (Tabs)

This row sits directly below the first and is separated by a full-width, thin bottom border (e.g., `border-b border-gray-200`). This row itself uses `flex` and `items-center`.

* **Active Tab ("Messages"):**
    * This component is a `flex items-center gap-2` with bottom padding (e.g., `pb-2`) and horizontal padding.
    * It is visually active, indicated by a **thick purple bottom border** (e.g., `border-b-2 border-purple-600`).
    * **Icon:** `MessageSquare` (Lucide), `text-black`.
    * **Text:** "Messages", `text-black font-semibold`.

* **Inactive Tab ("Untitled"):**
    * A `flex items-center gap-2` with the same padding, but styled as inactive.
    * The text and icon are grayed out (e.g., `text-gray-500`).
    * **Icon:** `File` or `FileText` (Lucide).
    * **Text:** "Untitled".

* **Add Tab Button:**
    * An icon-only button containing a `Plus` icon (Lucide), also styled as inactive (e.g., `text-gray-500`).

